{
  "name": "@ntsamaela/database",
  "version": "1.0.0",
  "description": "Database package for Ntsamaela platform",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "generate": "prisma generate",
    "push": "prisma db push",
    "migrate": "prisma migrate dev",
    "seed": "prisma db seed",
    "studio": "prisma studio",
    "reset": "prisma migrate reset",
    "setup": "ts-node setup.ts",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "setup:test": "ts-node setup.ts --test",
    "migrate:test": "prisma migrate deploy --schema=./schema.prisma",
    "seed:test": "prisma db seed --test"
  },
  "dependencies": {
    "@prisma/client": "^5.19.1",
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.8.0",
    "@types/bcryptjs": "^2.4.6",
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "prisma": "^5.19.1",
    "ts-jest": "^29.4.1",
    "ts-node": "^10.9.1"
  },
  "prisma": {
    "seed": "ts-node seed.ts"
  }
}
