{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "apps/web-admin/Dockerfile"
  },
  "deploy": {
    "startCommand": "SERVER_FILE=$(find /app -path '/app/node_modules' -prune -o -name 'server.js' -type f -print 2>/dev/null | head -1); if [ -n \"$SERVER_FILE\" ]; then cd \"$(dirname \"$SERVER_FILE\")\" && node \"$(basename \"$SERVER_FILE\")\"; else exit 1; fi",
    "healthcheckPath": "/",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
